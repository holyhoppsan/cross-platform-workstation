# GNOME extension placeholder

Phase 2 will add a GNOME Shell extension with an explicit supported-version list. No extension is packaged in Phase 1 because an unverified Shell API implementation would appear functional and can destabilize the GNOME session. The behavior contract is in `docs/quake-mode.md`.

