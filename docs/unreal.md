# Unreal integration status

Native Windows Unreal helpers are scheduled for Phase 5. No engine installation path will be hard-coded. Local configuration will supply engine, editor, Visual Studio, and project-specific paths. Git Bash helpers will convert paths explicitly and preserve arguments containing spaces.

