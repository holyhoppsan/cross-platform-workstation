#!/usr/bin/env bash
set -u
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
export XDG_CONFIG_HOME="$repo_root/chezmoi/dot_config"
exec "$repo_root/chezmoi/dot_config/workstation/doctor" "$@"

