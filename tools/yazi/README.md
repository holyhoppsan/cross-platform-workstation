# Yazi

Yazi support is implemented in Phase 5 through `chezmoi/dot_config/yazi/`, the `y` shell helper, WezTerm bindings, setup, reset, doctor checks, and tests.

Yazi is a terminal file manager, not an AI agent.
